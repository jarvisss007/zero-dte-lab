# Zero-DTE Lab — 2026-08-21 [0dte]

**Chain state.** 22 snapshots recorded so far (09:45:23 → 11:30:42 ET, session live at run time).
First snapshot 09:45:23: spot 765.64, forward 765.51, T 6.24h, 37 OTM quotes; implied close
distribution std 4.41 (0.58%), skew −0.71; ATM 766 straddle mid 2.87 → **implied move ±0.37%**.
Peak-OI strike **750**, sitting 2.05% BELOW spot.

**Today's calls: NONE — named bar, both metrics.**
`max_pain`: the pin is 2.05% below spot and its ±0.25% band (748.13–751.88) would need a 2%
collapse to hold. `breaks` here is arithmetic, not a prediction, on a metric this lab has already
published as mechanically won at the first snapshot. `implied_move`: barred until a discriminator
is pre-registered — seven sessions without one is a correct abstention, not a gap to fill.

**Session count — three numbers, and the middle one moved while the last one did not.**
**26 files on disk / 19 complete (open-to-close) / 16 admissible to the straddle test.**
08-20 finished the session cleanly (09:46 → 16:02) but its first snapshot returns "smile unfit" —
`quote_ts 09:30:04`, IVs not yet populated — so it joins 08-07 and 08-12 as **complete-but-unfit**
and the straddle sample gained nothing from a perfectly recorded day. A third failure mode, not
previously named: not days the recorder misses, but days it catches too early to be usable, and
the discriminator is `quote_ts`, not `fetched_at_et`. **16 of 60.** At 0.64 admissible per trading
day the gate projects to about **2026-11-25**, five days later than yesterday's estimate.

**Scorecard.** Ledger 8 rows, all scored, none pending. `implied_move` 3/3 (all `inside`);
`max_pain` 3/5. Neither is quotable: `inside` is expected to win 55–70% by construction, and the
first-snapshot geometry makes `breaks` near-automatic. The 08-20 `max_pain` row was council-scored
`right` off the settled 762.60 — **reconciled and verified, not re-scored** (BENCH-002).

**Forecasts.** Three overdue rows resolved off settled 08-20 closes, all one run late by
construction (SCHED-001): SPY>770.56 → **NO** (p 0.53) · SPY<777.88 → **YES** (p 0.45) ·
realized-vs-band → **YES** (p 0.38), realized −0.545% against a ±0.37% band, the **first OUTSIDE**
this repeated question has returned. Robust to the snapshot ambiguity: outside off the 09:46 spot
too. Running record on that question 1 OUTSIDE / 1 INSIDE, n=2 — p unchanged.
**Filed today:** the same question for the **2026-08-25** session at **p=0.38**, sixth posting
(08-21 and 08-24 are already claimed by open rows; a second row on a claimed session would count
one observation twice). n=12 resolved here; no calibration bin is actionable.

*Observation and calibration only. Never a trade. Elevated realized movement is not an edge until
it is shown to exceed what the options market charges — that test needs the 60-session gate.*
