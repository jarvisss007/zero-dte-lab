# Zero-DTE Lab — 2026-08-26 [0dte]

## Chain state
Today's file exists: `data/chains/SPY_2026-08-26.csv`, 19 snapshots, 09:57:38 → 11:27:53 ET (2,912 rows — still mid-session, the recorder is running). First snapshot 09:57:38, `quote_ts` 09:42:14, **151 of 152 rows carry a non-blank `iv`** — the honest IV test from 08-25, not the `quote_ts` second-mark heuristic that broke on 08-24.
First-snapshot read: spot 765.555, forward 765.58, T = 6.04h, 51 OTM quotes; implied close distribution std 5.24 (0.68%), skew −1.50; **ATM 766 straddle mid 3.19 → implied move ±0.42%**. Max pain 766.00 (+0.058% from first-snap spot), peak OI 765.00.

## Today's calls
**None, and not for want of a setup.** Max pain 766.00 sits *inside* the ±0.25% band this time — the opposite of yesterday's disqualification — so `holds` would be a genuine call rather than arithmetic. It is disqualified by the other bar instead: the file already holds **90 minutes of tape**. Spot has run +0.112% to 766.41, consuming 27% of the ±0.42% band, and is sitting essentially ON the 766.00 pin. Logging `holds` now would record where price already is. That is a real cost paid for the fourth run in a row, and it is the right one.

**Naming what the fix takes, as the council asked.** One function, one field: (1) a loader that returns only the rows whose `fetched_at_et` equals the session's first snapshot and *refuses* to return later ones — `implied_density.py --time` already picks one snapshot for its math, so what is missing is the refusal, i.e. the deciding process never seeing the later tape; (2) the call-writing step calling that loader instead of opening the file; (3) a `read_path: snapshot-only` column so post-fix rows are their own stratum, the way `minutes_into_session` is. Without (3) the fix cannot be verified after the fact. One afternoon of work; four forgone sessions and counting is the running cost.

## Session count
22 usable / 60 gate (as of 2026-08-25; 28 files, 6 stubs, definition = rows ≥ 86% of full-session median). Today's file is a partial and will only be classified once the recorder finishes. The alternate smile-fit definition reads 19/60. Gate lands ≈ 2026-10-30 (row-count) / ≈ 2026-11-17 (smile-fit) — both definitions still published, not the flattering one.

## Scorecard
`implied_move`: 3 scored, 3 right — but every one called `inside`, which is the metric's own base rate, so this is not evidence of a read.
`max_pain`: 5 scored, 3 right. Two of those five carry `minutes_visible_at_call` of 119 and 121 and are a separate contaminated stratum; never blended.
Pending ledger rows: 0.
Forecasts: **08-25 resolved 0** (realized −0.056% vs band ±0.31% → INSIDE; the question asked OUTSIDE). Filed today: 2026-08-28 OUTSIDE at **p = 0.38**, unadjusted — the 0.3–0.4 bin reads n=5, said 0.380, happened 0.200, gap −0.180; the gap clears 0.10 but n does not clear 30, so no adjustment is permitted. `resolves check_date+1 (standing, SCHED-001)`.
