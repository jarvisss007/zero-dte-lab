# Zero-DTE Lab — 2026-08-10 [0dte]

**Chain state.** Recorder LIVE and the schedule block has lifted: 21 snapshots
today from 09:45 to 11:26 ET, ~5-minute cadence, real greeks. This is the first
sweep in weeks where a same-day usable snapshot exists at run time, so this lab
is NOT blocked today. Earliest usable snapshot 09:45:55 ET (23 of 144 rows had
iv==0 — degraded but usable, unlike the all-zero first snapshot flagged 08-07).
Spot 773.01, implied close std 5.83 (0.75%), skew -1.89, ATM 773 straddle mid
2.84 => **implied move ±0.37% by the close**. Computed max pain **769.0**, with
spot sitting 0.52% above it.

**Rescored under the ratified spec.** The 07-17 `implied_move` row, marked at
ZDTE-001 for rescoring at the next sweep, is rescored today — the first weekday
sweep after the 08-09 ratification. Morning spot 741.17 (09:49 ET snapshot) to
close 743.29 = **+0.29% intraday** against an implied ±0.63%: `inside` is
**RIGHT**. Under the old close-to-close spec it was -0.99% and scored `wrong`.
**The verdict flipped.** Both verdicts stay on the record.

**Today's calls** (2, both check today, scored at the next run):
- `implied_move` **inside** — ±0.37% band = 770.15 to 775.87 from morning spot.
- `max_pain` **breaks** — close outside 767.08-770.92 (±0.25% of 769).

**Lookahead disclosure, and it is structural.** This run fires 11:28 ET, ~1.7h
into a 6.2h session, and at that moment SPY prints 773.78 — +0.10% off morning
spot, inside the band and 0.62% above max pain. Neither call is decided (the
remaining ~4.5h carries most of the variance, and the 08-07 case where the
outcome was already locked was correctly refused), but both are informed by
realized tape the morning snapshot did not contain. This is not a one-off: the
08:20 PDT trigger will put every future call ~1.7h into the session. Flagging it
for [coach]/Anupam — the fix is a call timestamped to the snapshot, not a change
to the metric.

**Session count: 16 recorded of the 60** the straddle-underpricing verdict needs.
That is the project's real progress bar and it moved by one today.

**Scorecard.** `implied_move` 1/1, `max_pain` 1/1, 2 pending (both today's).
n=2 total — no hit rate exists, and `inside` carries a 55-70% structural base
rate anyway, so beating a coin flip would mean nothing. The README verdict
stands: elevated realized movement is not an edge until it beats what the options
market charges, and that needs 44 more sessions.
