# Zero-DTE Lab — 2026-08-03

## Chain state
**No snapshot for today.** The recorder ran its full schedule and every fetch
failed with `URLError(gaierror(8, 'nodename nor servname provided'))` — 84
consecutive DNS failures from 09:31 to 16:06 ET. No `SPY_2026-08-03.csv` was
written. Last good capture: 2026-07-31. Cause is the machine's network (asleep
or off-network), not the data source.

## Today's calls
**None.** The rule is explicit: no snapshot, no calls, never predict from stale
chains. There is nothing to read a straddle or a pin off, and manufacturing a
call from Friday's chain would be exactly the self-deception this lab exists to
prevent.

## Session count
12 chain files on disk, but 07-08 (22KB), 07-21 (194KB) and 07-23 (148KB) are
partial captures against ~1.6MB for a complete session. **~9 usable sessions vs
the 60-session gate** for the straddle-underpricing verdict. That is the real
progress bar and it did not move today. Two of the last nine trading days have
been lost to the same DNS failure, so the effective collection rate is well
below one session per trading day.

## Scorecard
2 scored calls, 0 pending. `implied_move` 0/1, `max_pain` 1/1 — 1/2 overall.
Both from a single session on 2026-07-17. Nothing was due today. No hit rate at
this n means anything, and the `inside` base rate (~55-70% by construction) is
still the bar for that metric, not 50%.

## Forecast
SPY closes above 757.67 on 2026-08-05, p=0.52 — a stated no-view, since with no
chain there is no information behind it.

Not a trade, not advice. Elevated realized movement is not an edge until it is
shown to exceed what the options market charges, and that test still needs 60+
recorded sessions.
