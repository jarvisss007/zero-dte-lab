# Zero-DTE Lab — 2026-09-16 [0dte]

**Chain state.** `data/chains/SPY_2026-09-16.csv` exists, 22 snapshots at read time, first at
**09:47:49 ET** (17.8 min into session), quote_ts 09:30:22 — 17.5 min earlier, the normal
delayed-feed direction (§1: the stamp is about the DATA, and it is behind the fetch, as it
should be). Spot **759.59**, 152 quote rows in that snapshot, 59 OTM quotes.
Implied close distribution: **std 7.24 (0.95%)**, skew **−1.32**, in-band mass 1.211.
**ATM 760 straddle mid 5.04 ⇒ implied move ±0.66% by the close.** That is nearly double
yesterday's ±0.3385% — **FOMC lands 14:00 ET today and the chain is charging for it.**

## Today's calls: NONE. Bar named.
**No ledger row was logged, and the reason is the leak, not the tape.** Before this lab's step
the sweep read `india-radar/data/radar.json` for step 1; its strip carries **SPY last 760.56**
(fetched 08:24 PDT = 11:24 ET), **+0.128% against the 759.59 snapshot spot = 0.19× the band.**
Under ZDTE-004 a discretionary `implied_move` or `max_pain` call made after seeing that records
where price already is. So none was made.

**The council's OPEN, answered straight: this is the SAME vector as yesterday, and it is not a
lapse — it is the sweep's own step order.** `labs-morning-sweep` runs india-radar as step 1 and
zero-dte-lab as step 3, and india-radar's strip carries SPY **by construction** (it is one of
its ten global cues). **The leak therefore recurs on every single run and will keep recurring
until either the step order changes or the India step stops printing SPY.** Yesterday's
directive told this lab to "open nothing that carries live SPY before the filing is written" —
that instruction cannot be obeyed by a lab that is step 3 of a task whose step 1 prints SPY.
**Registered as a proposal to the council, not fixed here: reordering a scheduled task's steps
is a mandate change (REG-PP-001), not a sweep's call.** `recorder.log` was NOT tailed.
The full ordered list of files opened before the filing is written verbatim into row #20's
`notes`, as asked.

## Forecast filed (1 of 1) — #20, the pre-registered constant
**"SPY realized morning-spot-to-close move on 2026-09-16 lands OUTSIDE that session's
first-snapshot ATM-straddle implied move" — p = 0.38**, unchanged, pre-registered 2026-09-03.
Resolves check_date+1 (standing, SCHED-001 — a PASS by construction, never a deferral).
Band: |close/759.59 − 1| > 0.66%, i.e. close > 764.60 or < 754.58.
**This is the second consecutive day the pre-registration has bought something real**: a
constant fixed before the session cannot be contaminated by the session, so the book kept
filing on a day its discretionary side was correctly shut. CAL-001: `calibrate.py zero-dte-lab
0.38` returned 0.38 — p_cal == p, no adjustment.

## Scored
**#19 → OUTSIDE = NO (outcome 0).** SPY official 2026-09-15 close **757.39** vs morning spot
759.14 = **−0.2305%** against a ±0.3385% band — **inside, consuming 68% of it.** Settledness
proven on SPY's own tape (§9): a later bar exists, 2026-09-16. Not a clock argument.

## The registered readout — where it actually stands
Bin 0.30–0.40: **n=20 resolved, k=3 YES (rate 0.150), said 0.380, gap −0.230.** Under the
pre-registered Clopper-Pearson design, **k ≤ 5 at n=30 convicts the 0.38 prior; k ≥ 6 is
noise.** With **k=3 and 10 resolutions to go**, the prior is convicted unless **3 or more of
the next 10 land OUTSIDE**. Readout ~2026-10-01. Nothing may touch p before then, and the bin
is *not* actionable under CAL-001 regardless (n=20 < 30 — both n≥30 and |gap|>0.10 are
required, and only the second is met).
**§6 flag, small but real:** the row's own `FILING #N` counter and the bin's resolved `n`
disagree — today's row says #20 while the bin counts 20 already resolved, so #N is one ahead
of any count reproducible from the file. Two readers of one series. Nothing rests on it today
because the readout is defined on the **bin count**, not on #N; flagged so it is not
discovered at the readout.

## Session count — the real progress bar
**34 usable / 60 gate** (43 files, 9 stubs), recomputed from disk this run. Today's file is
correctly **excluded** — 3,345 rows against the 9,716-row bar, because the session is still
recording. 26 sessions to the straddle-underpricing verdict.

## Scorecard
Ledger: 8 rows, **0 due, 0 overdue, 0 scored** (last row 08-20; this lab's unit is the forecast
book now). Forecasts: 31 filed, 30 resolved, 1 pending (#20, resolves tomorrow).

*Council directive applied (zero-dte-lab, 2026-09-15): KEEP carried — #19 filed at the prereg
constant with the band read from the chain CSV, recorder.log not tailed. OPEN answered above,
with the mechanism rather than an apology. CLOSED honoured — **ZDTE-005 carries nothing**; no
ahead/behind is reported here, per the Resolver's rewrite.*
