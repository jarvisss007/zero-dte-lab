# 0DTE brief — 2026-09-24 (LATE CATCH-UP, written ~21:00 PT after the close) [0dte]

**Readout arithmetic (frozen 0.38 series):** bin n=24, k=5, 6 filings left — CONVICTED unless >=1 of the next 6 lands OUTSIDE. Unchanged today.

## Chain state
29 snapshots, first **13:42:23 ET** (252 min in; recorder skipped 09:31–09:51+ on a CBOE book still stamped 09-22 16:00). At that bar: spot 766.81, ATM 767 straddle mid 2.22 => +/-0.29% to close; implied std 0.37%, skew -0.48. File = 4,448 rows => STUB under the 86%-of-median rule.

## Today's calls
**None, and #24 of the 0.38 series NOT filed — named bar (ZDTE-003/004, Brain §29):** this run starts after the settled 09-24 close (767.18) is on the tape, so any 09-24 intraday row would be registered after its own outcome. The series skips 09-24; its bin does not advance.

## Forecast filed (1 of 1, forward-only)
|SPY 09-24 -> 09-25 close-to-close| > 0.75% — **p = 0.31** `[p_cal=0.31]`, base 156/500 = 0.312 (last 250: 0.308). No 09-25 print existed at filing. Resolves check_date+1 (standing, SCHED-001).

## Scored today
Nothing. Ledger: 0 open rows. The 09-23 row (09-23 -> 09-24 close) is not resolved tonight: SPY's tape has no later traded bar after 09-24 yet (§9/§18). Provisional arithmetic only: 767.81 -> 767.18 = -0.082%, would resolve NO — to be written by the next run once 09-25 trades.

## Session count
38 usable of the 60-session gate (session_count.json as_of 09-22); 09-23 had no chain, 09-24 is a stub — no progress.

## Scorecard
Forecast book: n=34 resolved, Brier skill -0.0412, reliability 0.0324, resolution 0.0258. **FCST-005 dispersion: last-25 p sd 0.039 (range 0.31–0.55)** — near-undispersed by pre-registration (Brain §14). Ledger lifetime 8 rows: `implied_move` 3/3, `max_pain` 2/5, 0 pending.

## §28 — recorder across sleep (directive answer)
`src/chain_recorder.py` checks its own `datetime.now(ET)` at invocation against `sessions.is_session` + a 09:29–16:06 ET window and exits otherwise, and it skips any book whose quote_ts is >30 min old — so a run resuming after sleep past 16:06 writes nothing. Residual: the clock is read once before the fetch, so a sleep mid-fetch would stamp pre-sleep `fetched_at` on a later book (future-dated quote_ts passes the age test). Reported, not changed. (There is no separate "daytype" recorder in this repo.)

## §29 — late retry
This run did the session-independent half (score check, forecast on an unopened session, brief) and refused the session-bound half (#24 off a 09-24 snapshot).

**Standing:** elevated realized movement is NOT an edge until it beats what the options market charges; that needs 60+ sessions. Never a trade.
