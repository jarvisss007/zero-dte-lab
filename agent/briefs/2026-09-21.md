# Zero-DTE Lab — 2026-09-21 [0dte]

**ZDTE-009 COLLISION, FIRST LINE AS DIRECTED — and today it is the largest it has ever been.**
Step 1 of the sweep (india-radar) read `data/radar.json`, whose strip carries **SPY 769.94**,
before this lab ran. Resolving #21 then required SPY's 09-18 close from an endpoint that also
served today's 09-21 bar. **SPY is +0.49% from the 766.21 first-snapshot spot — about twice
today's entire ±0.25% band already consumed before the filing was written.** Both reads are
structural to the sweep's order, which is not mine to change (REG-PP-001). ZDTE-009 is still
unruled; the ZDTE-004 conflict inside it is the council's and it said to resolve first anyway,
which is what I did. **p was NOT adjusted for the leak** — adjusting a pre-registered prior on
a leaked observation is worse than carrying the leak.

**Council: applied** — directive zero-dte-lab 2026-09-18. All three KEEPs carried: #21
resolved on pre-registered arithmetic before #22 was filed, the bin advanced, #22 filed at
0.38 untouched, and the #N-vs-bin agreement is stated rather than quietly reconciled.

**Chain state.** Today's snapshot exists: `data/chains/SPY_2026-09-21.csv`, **22 snapshots**
at read time. First snapshot **09:49:11 ET, 19.2 min into the session**; `quote_ts`
09:32:30, **16.7 min earlier** — the normal delayed-feed direction. Spot **766.21**, forward
766.14, 154 quote rows, 32 OTM quotes. ATM 766 straddle mid **1.89 → implied band ±0.25%**;
implied close std 0.39%, skew −1.53, in-band mass 1.033. **The band has halved again from
09-18's ±0.35% and is the tightest this book has ever filed against.**

**Today's calls: none.** The ledger metrics stay retired on this lab's own 2026-08-20 finding
— at the first snapshot spot is essentially never inside a ±0.25% band, so `max_pain` produces
`breaks` mechanically and measures a base rate rather than a call. 0 ledger rows due, 0 overdue.
The forecast book is where the implied-move question is asked now, and it was asked.

**Forecast.** Resolved **#21 → NO (0)**: 09-18 first-snapshot spot 761.34, band ±0.35%, close
761.69, realized **+0.046%** — comfortably inside. Settled by two readers (SPY's own tape
carries a later 09-21 bar; `sessions.settled_session()` = 2026-09-18). **Bin now n=22, k=4.**
Filed **#22 at 0.38, unchanged**. With 8 resolutions left, **the 0.38 prior is convicted
unless at least 2 of the next 8 land OUTSIDE.** The 0.3–0.4 bin reads said 0.38 / happened
0.19 / gap −0.19 and is not actionable (n<30); the pre-registration forbids moving p regardless.
**CAL-001:** `calibrate.py zero-dte-lab 0.38` → 0.38, **[p_cal=0.38]**, unchanged.

**Session count — the real progress bar.** 45 files, 36 usable, **29 admissible** against the
**60-session gate**. Today's file is still filling (3,376 rows at read time vs the 9,716-row
completeness bar) and is not yet counted. The straddle-underpricing verdict is under half-way
home; nothing here anticipates it.

**Scorecard.** Ledger: `implied_move` 4/4 right (n=4, all pre-fix or early-spec rows),
`max_pain` 3/5 (n=5, two of them carrying disclosed 119–121 minute leaks). Both far below
the 20-scored bar and both retired. Pending: 0.

**Firm Brain §25 — NEW THREAD answered, and the answer is clean.** `com.anupam.zerodte-recorder`
has **no `EnvironmentVariables` key at all** in its plist, so it runs under launchd's bare
default PATH. I ran the recorder's RTH + staleness guard path both ways:
- attended shell → `DRY-RUN fetch ok: 154 contracts parsed for 2026-09-21 (spot 770.16, quotes 2026-09-21T11:19:53)`, rc=0
- `env -i PATH=/usr/bin:/bin:/usr/sbin:/sbin` → **byte-identical output**, rc=0.
**Same behaviour.** The structural reason, which is the part worth recording: this recorder
**never shells out** — its staleness guard is a pure `datetime` comparison against the feed's
own `last_trade_time`, and its RTH guard is `ZoneInfo` against `/usr/share/zoneinfo`. It has
**zero exposure to ENV-001's vector**, because that vector is `git` and this job never calls it.
The git exposure in this lab is in the **agent's** off-machine-copy step (BAK-001), which runs
attended — exactly the §25 asymmetry, but on the safe side of it here.

**Off-machine copy (BAK-001):** committed and pushed at end of run; result stated in the sweep summary.
