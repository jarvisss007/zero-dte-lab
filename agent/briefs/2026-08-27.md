# Zero-DTE Lab — 2026-08-27 [0dte]

## Chain state
22 snapshots recorded so far today; **first snapshot 09:45:42 ET** — inside the ratified ≤09:50 stamp, so **ZDTE-003 did not recur** (yesterday's 09:57:38 reads as a slip, not a drift).
First-snapshot read: spot 768.47, forward 768.27, T = 6.24h, 32 OTM quotes. Implied close distribution std 4.65 (0.61%), skew −1.34. **ATM 768 straddle mid 3.12 → implied move ±0.41%.**

## Today's calls — NONE. Fifth consecutive priced refusal, on a NEW bar.
The first four refusals were "too many minutes visible". Today's is sharper: **I have seen the answer.** Resolving yesterday's forecast required the Yahoo SPY endpoint, whose current-day bar returns the live intraday last — **770.46 against the 09:45 snapshot spot of 768.47, i.e. +0.259%, 63% of the ±0.41% band already consumed.** Logging `inside` now would record where price already is.
`max_pain` is separately disqualified: peak OI at the first snapshot sits at **strike 747, 2.87% below spot** — a far-OTM open-interest artifact, not a pin. `breaks` would be arithmetic, not a call (this lab's own 2026-08-20 verdict).

## The snapshot-only read path, as specified, would not close the leak
The council's fix is three chain-side items: a first-snapshot-only loader, the call step calling it, and a `read_path` column. **The leak is not only chain-side.** Step 2 of this AGENT.md — score due calls — reaches a price endpoint that hands over today's tape before step 5 ever runs. Today it handed over 105 minutes of it. A loader hiding later chain rows would have left that fully intact **and stamped the resulting row `read_path: snapshot-only`** — converting an admitted leak into a certified one, which is worse than today's refusal.
The cheap fix that would actually work is a **run-order change: make the call before scoring** (step 5 ahead of step 2), so the call is written before anything touches a price feed. The loader still earns its place — it closes the chain half and makes the stratum verifiable — but it must not ship alone.
**Flagged, not changed.** This agent does not rewrite its own run order; that belongs with items (1)–(3) as a fourth item for the council/Anupam.

## Session count — the real progress bar
**23 / 60 usable sessions** (row-count definition, ≥86% of the full-session median, as of 2026-08-26); today makes 24 once the file completes. The stricter smile-fit count runs behind it, and the gate is the **later** of the two — both definitions stay published, not the flattering one.

## Scorecard
Ledger: 8 rows, all closed, **none due today.** `implied_move` and `max_pain` each carry their pre-fix/post-fix strata separately; nothing new to blend.
Forecasts: resolved `SPY … 2026-08-26 lands OUTSIDE` → **NO** (realized +0.069% vs ±0.42% band — 16% of the band). The OUTSIDE shape is now **1 of 7**.
Filed: same question for **2026-08-31, p = 0.38, unchanged.** The 0.30–0.40 bin now reads 1-of-7 (said 0.380, happened 0.143) — a large one-way gap, and at n=7 the rule forbids acting on it. Declining the adjustment is recorded on the row so the file shows it was considered and refused.
