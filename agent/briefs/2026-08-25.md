# Zero-DTE Lab — 2026-08-25 [0dte]

## Chain state
8 snapshots on disk at run time (09:47:55 ET first, 11:33:15 ET last) — with a **57-minute recording gap between 10:08 and 11:08**. First snapshot: spot **766.34**, quote_ts 09:32:05, implied std **0.46%**, ATM 766 straddle mid 2.35 → implied move **±0.31%**, skew **-1.12**.
Max-pain strike **764** (also peak-OI, 19,385 contracts) sits **+0.306%** from spot.

## Today's calls — NONE. Two named bars, each disqualifying alone.
1. **`max_pain` is mechanical.** +0.306% is outside the ±0.25% band at the first snapshot, so `breaks` is arithmetic, not a call — the exact defect this lab convicted the metric for. Yesterday's non-mechanical setup was the first on record; today is back to normal.
2. **Contamination.** The run fires 11:24 ET with ~123 minutes of session in the file; spot has walked 766.34 → 764.79, *inside* the ±0.31% band. An `implied_move: inside` row filed now would be outcome-informed, not snapshot-informed (ZDTE-002/003).
Ordering followed per the council's open ask: chain read → band and pin computed → call decided → **only then** the settled 08-24 close fetched. The refusal was made on the snapshot, not the tape. It still did not yield a loggable row, because the file carries two hours of tape whatever order it is read in — the ordering rule is necessary, not sufficient. The sufficient fix is a snapshot-only read path.

## Session count — recomputed from the files, both definitions named (CARD-001)
`session_count.py` was re-run, clearing the stale `as_of 2026-08-23` stamp.
- **Row-count definition** (≥86% of the 11,344-row full-session median → 9,755): **28 files / 22 usable / 6 stubs**. ← this is what `session_count.json` and the front page quote.
- **Smile-fit definition** (first snapshot has non-blank IVs): **22 complete / 19 admissible**; inadmissible are 08-07, 08-12, 08-20.
Progress to the 60-session straddle gate: **22/60** (row-count) or **19/60** (smile-fit). At the observed rate over the 27 trading days 07-17..08-24 — 0.815/day and 0.704/day — the gate lands ~**2026-10-30** and ~**2026-11-17** respectively.

**Correction to yesterday's brief:** the first-snapshot `quote_ts` second-mark is **not** the discriminator between IV-populated and IV-blank sessions. It held on the 08-20/08-21 pair and breaks on **08-24 (quote_ts 09:30:06, 111 IVs populated)**. The honest test is the direct one — count non-blank `iv` in the first snapshot.

## Forecast
**Resolved:** the 08-20 row asking whether 08-24 would land OUTSIDE → **NO**. 08-24 first snapshot spot 764.51, band ±0.34%; settled close 763.47; realized **-0.136%**. The straddle overpriced the day by **2.5×**. Six resolutions of this question: **five INSIDE, one OUTSIDE** — squarely the documented 55-70% base rate, so no tilt.
**Held:** the row checking 2026-08-25 resolves check_date+1 (standing, SCHED-001) — today's session is still open.
**Filed:** SPY realized morning-spot-to-close move on **2026-08-27** lands OUTSIDE that session's first-snapshot band, **p=0.38** — eighth consecutive posting of the identical uncontaminated question at the identical untilted probability.

## Scorecard
Ledger: `implied_move` 3/3 right (all `inside`, all pre-fix stratum) · `max_pain` 3/5 right (**and this lab's own verdict is that a first-snapshot `breaks` is mechanical, so those wins are not evidence**) · 0 pending.
Forecasts: 6 resolved on the OUTSIDE question, 3 pending (08-25, 08-26, 08-27).

## Flags
- **Dating defect:** the forecast row stamped `date = 2026-08-25` was actually filed 2026-08-21 (its note says "sixth consecutive posting" and cites the 08-20 resolution). Disclosed, **not edited** — a recorded row's fields are not a sweep's to rewrite. Any analysis reading `date` as the filing date will place one observation four days late.
- **Desk defect:** `score_forecasts.py --lab X` overwrites `calibration_table.json` with that lab alone, erasing every other lab. That is the single cause of the "no row for me" notes across three labs today.
