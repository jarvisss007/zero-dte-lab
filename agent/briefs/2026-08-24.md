# Zero-DTE Lab — 2026-08-24 [0dte]

**Chain state.** 22 snapshots recorded by 11:31 ET. First snapshot of the
session: quote_ts 09:30:06 / fetched 09:45:43, spot 764.51, 35 OTM quotes,
smile FITS. Implied close distribution std 0.90%, skew −2.57; ATM 764 straddle
mid 2.58 → implied move ±0.34% by the close. Max pain 766.0, peak OI 765.0
(25,668), then 766.0 (20,183) and 772.0 (18,368).

**Today's calls: NONE — named bar, and it cost a genuinely interesting row.**
`max_pain` had, for the first time on record, a NON-mechanical setup: spot
764.51 sits INSIDE the ±0.25% band of both candidate pins (766.0 →
764.09–767.92; 765.0 → 763.09–766.91), so `holds` would have been a real
question rather than the arithmetic `breaks` this lab convicted the metric for.
It was not logged because the run had already read SPY's live 763.63 print
while resolving the 08-14 forecast — a call written after seeing the tape is
outcome-informed, not snapshot-informed. Disclosed rather than dressed up; see
lessons for the runner defect behind it. `implied_move` stays barred until a
discriminator is pre-registered (eighth session).

**Session count.** **27 files on disk / 20 complete / 17 admissible**, against
the 60-session gate. Complete 19→20 and admissible 16→17, both from 08-21,
whose first snapshot fit a smile where 08-20's did not (`quote_ts` 09:30:15 with
IVs populated vs 09:30:04 with none). Complete-but-unfit set unchanged: 08-07,
08-12, 08-20. Honest projection: 0.654 admissible/trading-day → gate lands
around **2026-11-24**.

**Scored today.** One OVERDUE forecast, three runs late: the 08-14 row
(realized vs band on 08-21). Realized +0.0104% against ±0.37% → INSIDE →
OUTSIDE question NO, p was 0.38. That question is now 1 OUTSIDE / 2 INSIDE at
n=3 and p stays 0.38.

**Scorecard.** Ledger 8 rows, all scored, none pending. `implied_move` 3/3
(all `inside`, all pre-fix or early stratum). `max_pain` 3/5 — and the lab's own
verdict stands: at the first snapshot `breaks` is produced mechanically, so
that 3/5 measures a base rate, not skill. Forecasts n=13 resolved.

Council: applied — settled close used for the 08-21 resolution (Yahoo daily bar,
not the live quote); `implied_move` left barred with no discriminator; the
three-number session count recomputed from the files rather than incremented,
and the `quote_ts` discriminator reported as the thing that separated 08-20 from
08-21; no metric threshold loosened.
