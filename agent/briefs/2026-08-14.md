# Zero-DTE Lab — 2026-08-14 [0dte]

## Chain state
21 snapshots recorded, **first at 09:50:19 ET — 20 minutes into the session.** A clean recovery from 08-13's 10:27 start; the recorder was awake on time.
09:50 snapshot: spot 778.09, forward 778.11, T = 6.16h, 40 OTM quotes. Implied close distribution mean 777.83, **std 3.81 (0.49%)**, skew -1.11. **ATM 778 straddle mid 2.30 ⇒ implied move ±0.30% by the close** — the tightest band this lab has recorded (prior: 0.63, 0.37, 0.41).
Max pain / peak-pain strike from the same snapshot: **770**. Peak single-strike OI: 780 (97.9k), 788 (88.3k), 775 (85.6k).

## Today's calls — NONE. Two named bars, both session-independent.
**`implied_move`: barred by the 08-12 lesson (3), which is still unmet.** That lesson states plainly that a constant `inside` call carries zero information — it *is* the 55-70% base rate rather than a read of it — and that the metric needs a discriminator (implied vs trailing realized, or vs same-weekday realized) **pre-registered before it is used**. No such discriminator has been built. Logging a fourth `inside` would add a fourth free win to a 3/3 record that already means nothing.

**`max_pain`: spot sits 2.1 implied-sigma from the pin, which makes `breaks` a near-tautology.** Spot 778.09 vs max pain 770 is **1.05% away**, against a ±0.25% band of 768.08–771.92 and an implied std of 0.49%. `holds` would require a 2.1σ downside move by the close. This is the mirror of the 08-13 finding: that day taught that "already outside the band" is not a reason to call `breaks` at a *marginal* 0.27% distance; the same principle says a *trivial* 1.05% distance is not a call at all, it is arithmetic, and harvesting it would inflate `max_pain`'s record with a free win.

Both bars are properties of the metric specs, not of today's tape — declining on them creates no selection bias. **Contamination disclosure:** this run fired 11:24 ET, ~94 minutes into the session, so had a call been logged it would have been stamped `minutes_into_session = 20` (the 09:50 snapshot, per ZDTE-003) while the caller had seen 94 minutes. Those are different numbers and only the first was ever recorded.

**Schema fix applied:** ZDTE-003 was ratified 08-13 and mandated `snapshot_et` + `minutes_into_session` on every row, but the ledger header never gained the columns because no row was logged that day. Added today (blank for the five pre-rule rows, no recorded value touched), plus a third column `minutes_visible_at_call` — because `minutes_into_session` discloses how contaminated the *price* was, and nothing yet discloses how contaminated the *caller* was.

## Session count
**21 of the 60+ sessions** the straddle-underpricing verdict requires. That gate is the project; these calls are side calibration and do not shorten it.

## Scorecard
`implied_move` 3/3 — expected, not skill; `inside` wins 55-70% by construction and 3 rows cannot beat that bar. `max_pain` 2/3. **0 pending.** No hit rate here is a result.

## Forecast (mandatory, one per run)
**Not resolved today, deliberately.** The 08-05 row ("SPY closes above 771.33 on 2026-08-14", p=0.55) is due, but this run fired 11:24 ET with the session open — the 08-14 close does not exist yet. It resolves on the next run from the settled 08-14 close. Fabricating a close off an in-progress bar is barred outright.
Written: **"SPY's realized morning-spot-to-close move on 2026-08-21 lands OUTSIDE that session's first-snapshot ATM straddle implied move", p=0.38.** First forecast in this book that asks *this lab's own question* rather than a generic SPY-close question, and the first with p meaningfully off 0.5 — the documented 55-70% `inside` prior gives a real basis for 0.38.

Standing: elevated realized movement is NOT an edge until shown to exceed what the options market charges. Never a trade.
